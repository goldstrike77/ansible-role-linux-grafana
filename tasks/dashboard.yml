---
- name: Grafana dashboard file transfer
  copy:
    src: 'dashboards/{{ grafana_dashboards }}/'
    dest: '{{ grafana_path }}/grafana/dashboards/{{ grafana_dashboards }}/'
    owner: 'grafana'
    group: 'grafana'
    mode: 0644
  with_items: '{{ grafana_dashboards }}'